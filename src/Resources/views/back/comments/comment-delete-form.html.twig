<div class="modal-form-container">
    <div class="row h-100">
        <div class="col mx-auto h-100">
            <div class="card-modal">

                <div class="card col-lg-5 col-md-8 py-4">
                    {#Modal closer#}
                    <span class="close-modal">x</span>

                    <div class="card-header border-0 mb-3 py-0 text-center">
                        <h1 class="text-black-50">Supprimer le commentaire de <span class="text-danger">{{ article_comment.authorName }}</span></h1>
                    </div>

                    <form role="form" action="{{ url('api_article_comment_delete') }}" method="post" class="ajax_form">

                        <input value="{{ article_comment.uniqKey }}" name="article_comment_id" type="hidden">

                        <div class="text-center text-black-50 small">
                            {{ article_comment.comment }}
                        </div>

                        <div class="alert alert-secondary text-center">
                            Souhaitez-vous vraiment supprimer ce commentaire ?
                        </div>

                        <div class="text-center">
                            <div class="form-response"></div>
                            <div class="row justify-content-center">
                                <button type="reset" class="btn btn-light mt-4 mx-3">ANNULER</button>
                                <button type="submit" class="btn btn-danger mt-4 mx-3 btn-submit">SUPPRIMER</button>
                            </div>
                            <div class="loader mx-auto my-3 d-none"></div>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>
</div>