<style type="text/css">
    .hidden-input{
        width: 1px;
        height: 1px;
        transform: scale(0,0);
    }

    .trombino-label{
        height: 100%;
        width: 100%;
        display: flex;
        position: relative;
        vertical-align: center;
        cursor: pointer;
    }

    .trombino-label .previewer{
        position: absolute;
        display: block;
        width: 100%;
        height: 100%;
        overflow: hidden;
        border-radius: 3px;
        border: 1px dashed rgba(16, 38, 122, 0.56);
        background-color: rgb(255, 255, 255);
    }

    .trombino-label.rounded, .trombino-label.rounded .previewer{
        border-radius: 50%;
        overflow: hidden;
        background-position: center;
        display: flex;
        justify-content: center;
    }

    .trombino-label.loaded .previewer{
        border: 1px dashed rgba(0, 201, 80, 0.56);
        background-color: rgba(224, 228, 233, 0.4);
    }

    .trombino-label .loader{
        display: none;
        z-index: 1;
    }

    .trombino-label.loading{
        background-color: #fff;
    }

    .trombino-label.loading .previewer *{
        display: none;
    }

    .trombino-label.loading .loader{
        display: block;
        position: absolute;
        width: 40px;
        height: 40px;
        left: calc(50% - 20px);
        top: calc(50% - 20px);
        z-index: 5;
    }

    .trombino-label .previewer:hover{
        background-color: rgba(255, 255, 255, 0.99);
        transition-duration: 200ms;
    }

    .trombino-label .previewer img{
        position: relative;
        display: block;
        margin-left: auto;
        margin-right: auto;
        width: auto;
        height: 100%;
        border: none !important;
        box-shadow: none;
        object-fit: contain !important;
        transform: scale(1.1, 1.1);
    }

    .trombino-label .previewer.full img{
        width: 100%;
        height: 100%;
        text-align: center;
        object-fit: cover !important;
        transform: scale(1.1, 1.1);
    }

    .trombino-label .previewer canvas{
        position: relative;
        width: 100%;
        height: auto;
        border: none !important;
        object-fit: cover;
        box-shadow: none;
    }

    .trombino-label .label-text, .trombino-label .label-front{
        position: relative;
        padding: 12px;
        background-color: rgba(255, 255, 255, 0.98);
        text-align: center;
        display: none;
        width: 250px;
        margin: auto;
    }

    .trombino-label .label-front{
        display: block;
        opacity: .8;
        background-color: rgba(255, 255, 255, 0.98);
    }

    .trombino-label.loaded .label-front{
        display: none;
    }

    .trombino-label.editable:hover .label-text{
        display: block;
        opacity: .8;
    }

    .trombino-label.editable:hover .label-front{
        display: none;
    }
</style>