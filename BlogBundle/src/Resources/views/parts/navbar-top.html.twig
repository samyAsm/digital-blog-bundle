<section id="topbar" style="z-index: 100;background-color: #2f2f2f;position: fixed;height: 30px;top: 0;right: 0;left: 0;">
    <div class="container">
        {% if not author() %}
            <a href="{{ url('digital_blog_login') }}" class="small float-right">{{ "Connexion"|trans }}</a>
        {% else %}
            <a href="{{ url('digital_blog_logout') }}" class="small float-right">{{ "Déconnexion"|trans }}</a>
            <a href="{{ url('digital_blog_article_list') }}" class="small float-right">{{ "Mon espace"|trans }}</a>
        {% endif %}
    </div>
</section>